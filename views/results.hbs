
{{#each meals}}
    <div class="item">
      <img class="results-image" src="{{image}}" alt="poll results image">
      <div class="results-text">
        <h3>{{name}}</h3>
        <h3>Votes:{{voters.length}}</h3>      
        <p class="voters-list">Voters:{{#each voters}}
        {{this}}<span>,</span>
          {{/each}}
        </p>
      </div>
    </div>
{{/each}}
  <input type="submit" value="Reset poll">
